import React from 'react';
import Search from 'component';
import SearchButton from 'component';

const SearchBlock = () => {
    return (
        <div>
        <Search />
        <SearchButton />
        </div>
    );
};